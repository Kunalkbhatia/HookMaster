{
  "name": "hookmaster",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "build": "tsc",
    "jest": "test",
    "lint": "eslint --fix --cache",
    "prepare": "husky",
    "pretty": "prettier --cache --write . && sort-package-json",
    "refresh": "rm -rf dist node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@manufac/eslint-config": "^4.2.1",
    "@manufac/prettier-config": "^1.0.6",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "husky": "^9.1.6",
    "jest": "^29.7.0",
    "sort-package-json": "^2.10.1"
  }
}
